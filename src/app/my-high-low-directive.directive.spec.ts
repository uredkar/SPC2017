/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MyHighLowDirective } from './my-high-low-directive.directive';

describe('Directive: MyHighLowDirective', () => {
  it('should create an instance', () => {
    let directive = new MyHighLowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

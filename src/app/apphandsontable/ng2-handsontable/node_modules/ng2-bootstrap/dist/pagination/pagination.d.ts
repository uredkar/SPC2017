/// <reference path="../../tsd.d.ts" />
import { OnInit, ElementRef, DefaultValueAccessor, NgModel, Renderer } from 'angular2/angular2';
export declare class Pagination extends DefaultValueAccessor implements OnInit {
    config: any;
    private classMap;
    private maxSize;
    private rotate;
    private boundaryLinks;
    private firstText;
    private previousText;
    private nextText;
    private lastText;
    private disabled;
    private directionLinks;
    private numPages;
    private pageChanged;
    private _itemsPerPage;
    private _totalItems;
    private _totalPages;
    private inited;
    private itemsPerPage;
    private totalItems;
    private totalPages;
    page: number;
    private _page;
    private pages;
    constructor(cd: NgModel, renderer: Renderer, elementRef: ElementRef);
    onInit(): void;
    writeValue(value: number): void;
    private selectPage(page, event?);
    private getText(key);
    private noPrevious();
    private noNext();
    private makePage(number, text, isActive);
    private getPages(currentPage, totalPages);
    private calculateTotalPages();
}
export declare class Pager extends Pagination implements OnInit {
    private align;
    config: {
        itemsPerPage: number;
        previousText: string;
        nextText: string;
        align: boolean;
    };
    constructor(cd: NgModel, renderer: Renderer, elementRef: ElementRef);
}
export declare const pagination: Array<any>;

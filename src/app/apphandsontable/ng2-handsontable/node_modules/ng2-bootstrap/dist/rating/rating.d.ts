/// <reference path="../../tsd.d.ts" />
import { OnInit, DefaultValueAccessor, NgModel, Renderer, ElementRef } from 'angular2/angular2';
export declare class Rating extends DefaultValueAccessor implements OnInit {
    private max;
    private range;
    private value;
    private preValue;
    private titles;
    private stateOn;
    private stateOff;
    private readonly;
    private ratingStates;
    private onHover;
    private onLeave;
    constructor(cd: NgModel, renderer: Renderer, elementRef: ElementRef);
    onInit(): void;
    writeValue(value: number): void;
    private buildTemplateObjects(ratingStates, max);
    private rate(value);
    private enter(value);
    private reset();
    private onKeydown(event);
}

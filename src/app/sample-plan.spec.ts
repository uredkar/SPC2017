/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {SamplePlan} from './sample-plan';

describe('SamplePlan', () => {
  it('should create an instance', () => {
    expect(new SamplePlan()).toBeTruthy();
  });
});
